import * as React from 'react'
import styled from 'styled-components/native'

// Components
import ScrollableHeader from '../components/ScrollableHeader'

const HomeContainer = () => {
    return (
        <ScrollableHeader title="Home">
            
        </ScrollableHeader>
    )
}

export default HomeContainer